# 0-sizer プロジェクト：Claude 用恒久メモ

このファイルは Claude Code の毎セッション開始時に自動で読み込まれます。
会話のコンパクションで揮発させたくない**運用ルール**と**プロジェクトの文化**をここに置きます。

---

## 運用ルール（必須）

### 1. 着手宣言を必ず最初の 1 メッセージで出す
新しいタスクを受け取ったら、何より先に「これから A → B → C をやります、終わったら報告します」と**冒頭で宣言**してから実装に入る。

理由：
- 沈黙したまま長時間作業して、その間ユーザーに進捗が見えないと、誤解やタイムアウトの原因になる
- 期待アクション（実装 → ビルド → commit → push → 報告）を共有しておくことで、途中で止まる確率が下がる

### 2. 沈黙で終わらない
作業中・監視待ち・思考中であっても、必ず最後に一言は出す。
監視タスクの完了通知で終わらせず、ユーザーが次に何をすればいいかも添える。

### 3. 空応答／"No response requested." を返さない
これは過去にバグ的にやってしまった失敗。ユーザーから「進捗どうですか？」が来る原因になる。
入力に対しては必ず何らかの実装着手 or 状況応答を返すこと。

### 4. デプロイは monitor で待つ
GitHub Pages デプロイは `Monitor` ツールで `index-*.js` の hash 変化を見て待つ。
完了通知が来たら「デプロイ完了 → `<新hash>`、強制リロードを」と返す。

---

## プロジェクト文化（ユーザーの一貫した好み）

### 教え方
- **音楽用語・シンセ概念をチャットで説明しない**。アプリ内で教える方針。チャットでは実装の話だけ
- レッスンは「永字八法」メタファ。1 曲「Eternal」が全要素を内包する設計を狙う

### 開発スタイル
- **小さく刻む**。1 機能ずつ実装 → デプロイ → ユーザー実機確認 → 次へ
- **main に直接 push**。PR は明示要求があるまで作らない
- 返事は日本語
- **AskUserQuestion ピッカーは使わない**（チャットのテキストが見えにくくなるから）
- コミットメッセージにモデル ID やセッション URL を入れない

### 制約
- クラウド環境で動いている。ブラウザを開けない・音を聞けない
- 動作確認は実機ユーザー（iPhone）に依頼する形になる

---

## 現状アーキテクチャ（簡易）

- Vite + React 19 + TypeScript、GitHub Pages にデプロイ
- 音源：Web Audio API、`useSynth()` を 2 つ（トラックごと独立 AudioContext）
- SEQ：16 ステップ × 14 行（白鍵 C3-B4）、2 トラック、タイ／スイング／タップテンポ／MUTE-SOLO／CUTOFF オートメーション
- 永続化：localStorage（`0sizer.*` キー群）
